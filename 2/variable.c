#include <stdio.h>

int
main(int argc, char *argv[])
{
    // 変数宣言
    int number;

    // 変数numberに12345を代入
    number = 12345;

    // 変数numberが保持している値を画面に出力
    printf("number is %d\n", number);

    return 0;
}
